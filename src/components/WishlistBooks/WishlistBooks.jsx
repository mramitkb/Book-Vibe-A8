import { useLoaderData } from "react-router-dom";

const WishlistBooks = () => {
    const wishlist = useLoaderData([])
    return (
        <div>
            <h3>ok</h3>
        </div>
    );
};

export default WishlistBooks;